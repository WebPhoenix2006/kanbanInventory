export interface Product {
    name: string;
    id: string;
    category: string;
    price: number;
    quantity: number;
    unit: number;
    exp_date: string; // Format: 'YYYY-MM-DD'
    threshold: string;
}
